# Sanctum OS — compressed RAM swap instead of disk swap.
# Faster under memory pressure and never writes secrets to disk unencrypted.
[zram0]
zram-size = min(ram / 2, 4096)
compression-algorithm = zstd
swap-priority = 100
