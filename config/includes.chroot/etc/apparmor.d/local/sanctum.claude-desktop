# Site-specific additions and overrides for the Sanctum Claude Desktop
# profile (/etc/apparmor.d/sanctum.claude-desktop). Rules added here are
# merged into the "claude-desktop" profile on reload:
#   apparmor_parser -r /etc/apparmor.d/sanctum.claude-desktop
